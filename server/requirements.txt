fastapi==0.140.0
# Pinned explicitly rather than left to fastapi, which only floors it at
# >=0.46.0 -- below the fixes for several advisories. Also the floor for the
# TemplateResponse(request, name, context) signature the web routes now use.
starlette==1.3.1
# Held at 0.24.0 deliberately: no advisory affects it, and it is independent of
# the starlette upgrade (uvicorn is the ASGI server; it does not import
# starlette). Worth bumping on its own, not as part of a security change.
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32  # required by FastAPI File/Form upload endpoints
slowapi==0.1.10
jinja2==3.1.6

# Development tools
mypy==1.13.0
pytest==9.0.3
# TestClient dependency. Pinned like everything else here: starlette now
# deprecates httpx for the TestClient in favour of httpx2, and starlette[full]
# caps it at <0.29, so an unpinned 0.29 could break the suite out of nowhere.
httpx==0.28.1
